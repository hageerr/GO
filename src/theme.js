import { createTheme } from '@mui/material/styles'; 


const theme = createTheme({
  palette: {
    primary: {
      main:  '#45a247',
      dark: '#222d31',
    },
    secondary: {
      main: '#f9f9f9',
      dark: '#eceff4',
      extraDark: '#a4a3ac',
    },
    
  },
});

export default theme ;